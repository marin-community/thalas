name: Run unit tests

on: [push, pull_request]

jobs:
  build:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository)

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10"]
        node-version: ["20.10.0"]

    steps:
      - name: Checkout code 
        uses: actions/checkout@v3

      - name: Set up Conda
        uses: conda-incubator/setup-miniconda@v2
        with:
          auto-update-conda: true
          python-version: ${{ matrix.python-version }}
          activate-environment: marin_unit_test_env
          auto-activate-base: false

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        shell: bash -l {0}
        run: |
          conda install -c conda-forge pandoc
          npm install -g pandiff
          pip install -e ".[dev]"

      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v1
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.NEW_GCP_JSON }}

      - name: Install Ray and other dependencies
        run: |
          pip install ray[default] google-api-python-client
          
      - name: Start Ray cluster on GCP
        shell: bash -l {0}
        run: |
          ray start --head

      - name: Verify installations
        shell: bash -l {0}
        run: |
          conda list
          pandoc --version
          pandiff --version
          python --version

      - name: Test with pytest
        shell: bash -l {0}
        run: |
          PYTHONPATH=tests:. pytest --tb=no -v